put qemu guest agent on all vms
add dns server like pihole with 1.1.1.1

rename gateway node to gateway host

netbird timeout for having to re log in on my mac should be longer it seems like 24hrs rn

rename k3s-control-plane-vm01 node name in k3s (probably just control-plane)