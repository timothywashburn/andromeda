next feature buildout
- truenas snapshots + r2 backup
- k3s: grafana + prometheus + loki + alloy
- k3s: authentik
- k3s: argocd?
- pterodactyl vm

possible future buildout
- k3s: shlink
- k3s: gitlab

make midclt bootstrap use the same format as the other midclt payload argv calls

bugs
- destination dat must be deleted before ip aliases can be deleted

move reusable code (opnsense firewalls, etc) to reusable role for an intended runtime configure playbook

put qemu guest agent on all vms

extra ambitious
- get source ips working by removing masquerade tunnel system

should hairpinning be for all internal services not just 80/443 to .253?

rename gateway node to gateway host (unsure if this has to be on rebuild probably not?)

scrub public ip and make this repo public

ensure that all traffic goes through gateway

clean up numbering and associated stuff in bootstrap tasks

test to make sure opnsense is still secure with ssh on

allow anyone to have netbird access for adblocking?

right now I'm doing the following (see below); wondering if metallb would be better. seems like this could be binding to all internal ips. maybe that's ok?
k3s_traefik: "{{ hostvars[groups.k3s_control_plane | first].ansible_host }}:80"

look at on rebuild
- check todos (I think there's at least one there)
- rename truenas thing from main